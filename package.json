{
  "name": "maccmsrust",
  "version": "1.0.0",
  "description": "Hugo site",
  "scripts": {
    "build": "hugo --gc --minify",
    "build:production": "hugo --gc --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "rtlcss": "^4.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}